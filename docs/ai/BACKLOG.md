# Project Backlog

## UI/UX Improvements
- [ ] **Server-Side Folder Picker**: Implement a directory browser in the Settings UI. This requires a backend endpoint to list local directories and a frontend modal to navigate the file system, allowing users to select output paths without manual typing.
- [ ] **Advanced Metadata Viewer**: Expand the image gallery modal to show full generation metadata (including LoRAs, Clip Skip, etc.) in a structured way.
- [ ] **Search in History**: Add a text-based search to filter history by prompt keywords.

## Technical Tasks
- [ ] **Model Configuration Editor**: Allow editing of `.json` sidecar files for models directly from the UI.
- [ ] **Progressive Image Loading**: Implement low-res previews or progressive loading for the image gallery to improve performance with large histories.
